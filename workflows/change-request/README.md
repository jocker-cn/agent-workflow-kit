# Change request workflow contract

`workflow.yaml` defines the durable business contract for the CR Skill: ordered phases, fields and evidence required before review, and the explicit `submit` confirmation gate. It intentionally contains no URLs, selectors, credentials, or page-specific rules.
